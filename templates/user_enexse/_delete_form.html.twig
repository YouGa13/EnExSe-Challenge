<form method="post" action="{{ path('app_user_enexse_delete', {'userid': user_enexse.userid}) }}" 
                        onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_enexse.userid) }}">
    <button class="btn">Delete</button>
</form>
